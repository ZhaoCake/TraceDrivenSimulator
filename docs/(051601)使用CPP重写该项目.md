# 使用CPP重写该项目

为了加快开发节奏，现放弃使用rust，改用cpp进行开发。

重写该项目为cpp。需求使用gtest代替cargo test，并保留各模块逻辑，但完全转化为cpp所使用的方式。需要使用的环境使用flake.nix描述，并且删除原本rust环境的相关内容。文件后缀全部使用.cc，注释全部用中文。

重写成果回复写入到该文件中。 [重写成果](#重写成果)之前的内容不可更改。

## 重写成果